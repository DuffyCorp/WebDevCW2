<html>
    <head>
        {{>header}}
    </head>
    <body>
    <!-- Jumbotron -->
    <div class="p-5 text-center bg-light">
        <h1 class="mb-3">{{title}}</h1>
        <h4 class="mb-3">Guest book app from lab work.</h4>
        <a class="btn btn-primary" href="/new" role="button">Add post</a>
    </div>
    <!-- Jumbotron -->
        {{#entries}}
        <div class="card">
            <div class="card-body">
                <div class="card-title">
                    <h4>Menu: <a href="/menu/{{menu}}">{{menu}}</a></h4>
                </div>
            </div>
        </div>
        {{/entries}}
        {{^entries}}
            No entries yet.
        {{/entries}}
        <div>
        </div>
    </body>
    <footer>
        {{>footer}}
    </footer>
</html>