<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
     <div class="wrapper">

        {{>sidebar}}

        <!-- Page Content  -->
        <div id="content" style="width 100%">
            {{>header}}
    <div class="HeroImage">
        <div class="heroText">
            <h1>{{title}}</h1>
            <h4>CW2 Resaurant webapp.</h4>
            <a id="login" href="#menus" class="btn btn-primary">Menus</a>
        </div>
    </div>
    <div class = "bodyContent" id="menus">
            <h1>Our menu's</h1>
        {{#entries}}
        {{^user}}
        <div class="card">
            <div class="card-body">
                <div class="card-title">
                    <h4>Menu: <a href="/menu/{{menuName}}">{{menuName}}</a></h4>
                </div>
            </div>
    </div>
        {{/user}}
        {{#user}}
        <div class="card">
            <div class="card-body">
                <div class="card-title">
                    <h4>Menu: <a href="/menu/admin/{{menuName}}">{{menuName}}</a></h4>
                </div>
            </div>
    </div>
        {{/user}}
        {{/entries}}
        {{^entries}}
            No entries yet.
        {{/entries}}
        </div>
    </body>
    <footer>
        {{>footer}}
    </footer>
        </div>
    
</html>