<html>
    <body>
     <div class="wrapper">

        {{#user}}
            {{>sidebar}}
        {{/user}}
        <!-- Page Content  -->
        <div id="content" style="width 100%" class="{{admin}}">
            {{>header}}

        <div class="Hero">
            <div class="heroText">
                <h1>{{title}}</h1>
            </div>
        </div>
            <div class = "bodyContent" id="menus" style="text-align: center">
        <h2>Delete a staff member</h2>
        {{#staff}}
                                                   <div class="login">
            <div class="AdminForm">
        <form method="post" role="form">
            <div class="form-outline mb-4">
            <div class="form-group">
                <label for="newMenuName" id="oldMenuName" name="staffUserName">Old menu name</label>
                <input type="text" class="form-control" name="staffUserName" value="{{user}}" readonly="readonly">
            </div>
            </div>

            <div class="form-group">
                <input type="submit" value="Delete Staff" class="btn btn-primary">
            </div>
        </form>
        </div>
        </div>
        {{/staff}}
    </body>
        </div>
            <footer>
        {{>footer}}
    </footer>
       <script src="/js/admin.js"></script>
</html>